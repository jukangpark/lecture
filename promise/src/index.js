import React from "react";
import ReactDOM from "react-dom/client";
import PromiseTest from "./PromiseTest";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(<PromiseTest />);
